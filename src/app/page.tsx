

export default function PageHome() {
  return (
    <main className="min-h-screen bg-background p-8 text-text">
      <section className="mx-auto max-w-xl rounded-xl bg-paper p-8 shadow-sm">
        <h1 className="text-3xl font-bold text-primary">CodarSe</h1>
        <p className="mt-2">As cores personalizadas do Tailwind estão funcionando.</p>
        <button className="mt-6 rounded-lg bg-primary px-4 py-2 font-semibold text-primary-contrast">
          Começar
        </button>
      </section>
    </main>
  );
}
